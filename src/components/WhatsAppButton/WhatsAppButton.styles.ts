import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#25D366",
    paddingVertical: 14,
    borderRadius: 14,
    alignItems: "center",
    marginTop: 28,
    marginBottom: 40,
  },

  text: {
    color: "#FFFFFF",
    fontSize: 16,
    fontWeight: "800",
  },
});
